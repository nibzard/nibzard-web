---
interface Props { delay?: number; duration?: number; yOffset?: number; className?: string; }
const { className = '' } = Astro.props;
---
<div class:list={['content-reveal', className]}><slot /></div>
