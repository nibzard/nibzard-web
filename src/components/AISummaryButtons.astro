---
// ABOUTME: AI Summary Buttons Component
// ABOUTME: Provides links to 5 AI platforms with pre-filled prompt to summarize the nibzard blog

const prompt = `Summarize and explain the blog at nibzard.com. Include:
- What the blog is about (main themes, topics)
- Who the author is (background, perspective)
- Key learnings and insights from the content
- Notable articles or posts worth reading
- The overall value proposition for readers

IMPORTANT: You can access the blog content efficiently by:
1. Fetching individual articles as plain text by appending ".md" to article URLs (e.g., nibzard.com/agent-stress-test becomes nibzard.com/agent-stress-test.md)
2. Accessing all content at nibzard.com/llms-full.txt (full archive)
3. Accessing recent content at nibzard.com/llms.txt

Use these endpoints to read the actual content and provide a more accurate, detailed summary based on the real source material rather than browsing the HTML site.`;

const encodedPrompt = encodeURIComponent(prompt);

const aiPlatforms = [
  {
    name: 'ChatGPT',
    url: 'https://chat.openai.com/',
    icon: '/assets/chatgpt.svg'
  },
  {
    name: 'Claude',
    url: 'https://claude.ai/new',
    icon: '/assets/claude.svg'
  },
  {
    name: 'Gemini',
    url: 'https://gemini.google.com/',
    icon: '/assets/gemini.svg'
  },
  {
    name: 'Grok',
    url: 'https://x.com/i/grok',
    icon: '/assets/grok.svg'
  },
  {
    name: 'Perplexity',
    url: 'https://www.perplexity.ai/',
    icon: '/assets/perplexity.svg'
  }
];
---

<div class="ai-summary-section">
  <p class="ai-summary-label">Request an AI summary of this blog</p>
  <div class="ai-buttons-row">
    {aiPlatforms.map(platform => (
      <a
        href={`${platform.url}?q=${encodedPrompt}`}
        target="_blank"
        rel="noopener noreferrer"
        class="ai-button"
        aria-label={`Summarize with ${platform.name}`}
        title={`Summarize with ${platform.name}`}
      >
        <img
          src={platform.icon}
          alt={platform.name}
          class="ai-button-icon"
          loading="lazy"
          width="40"
          height="40"
        />
      </a>
    ))}
  </div>
</div>

<style>
  .ai-summary-section {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    padding: 4rem 0 var(--space-lg) 0;
    text-align: center;
  }

  .ai-summary-label {
    font-family: var(--font-body);
    font-size: var(--font-size-base);
    color: var(--color-text-muted);
    margin: 0;
  }

  .ai-buttons-row {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    flex-wrap: wrap;
    justify-content: center;
  }

  .ai-button {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    overflow: hidden;
    padding: 0.375rem;
    text-decoration: none;
    transition: transform var(--transition-fast);
    cursor: pointer;
  }

  .ai-button:hover {
    transform: scale(1.1);
  }

  .ai-button:focus-visible {
    outline: 2px solid var(--color-accent1);
    outline-offset: 2px;
    border-radius: 50%;
  }

  .ai-button-icon {
    width: 100%;
    height: 100%;
    object-fit: contain;
    filter: brightness(0);
    opacity: 0.8;
    transition: opacity var(--transition-fast);
  }

  .ai-button:hover .ai-button-icon {
    opacity: 1;
  }

  @media (max-width: 480px) {
    .ai-buttons-row {
      gap: var(--space-sm);
    }
  }
</style>
