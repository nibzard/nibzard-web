<nav class="site-nav">
  <ul>
    <li><a href="/" aria-label="Home">~/</a></li>
    <li><a href="/log" aria-label="Log">log</a></li>
    <li><a href="/about" aria-label="About">about</a></li>
    <li><a href="/now" aria-label="Now">now</a></li>
  </ul>
</nav>

<style>
  .site-nav ul {
    display: flex;
    gap: var(--space-lg);
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .site-nav a {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    text-decoration: none;
    color: var(--color-text-muted);
    transition: all var(--transition-fast);
    position: relative;
    display: inline-block;
  }

  /* Terminal-inspired navigation */
  .site-nav a::before {
    content: "";
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 1px;
    background-color: var(--color-accent2);
    transition: width var(--transition-fast);
  }

  .site-nav a:hover::before,
  .site-nav a:focus::before,
  .site-nav a.active::before {
    width: 100%;
  }

  .site-nav a:hover,
  .site-nav a:focus,
  .site-nav a.active {
    color: var(--color-text);
    text-decoration: none;
    transform: translateY(-1px);
  }
  
  /* Terminal-style home link */
  .site-nav a[aria-label="Home"] {
    color: var(--color-accent2);
  }
  
  @media (max-width: 480px) {
    .site-nav ul {
      gap: var(--space-md);
    }
  }
</style>