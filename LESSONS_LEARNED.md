# LOG

## Instructions
This log tracks all tasks completed in this project. Use one record per task with the following format:
- **Timestamp**: Date in YYYY-MM-DD format
- **Task**: Brief description of what needed to be done
- **Action**: Specific steps taken, including key commands/tools used
- **Result**: Outcome and any relevant metrics/details

Add new entries at the bottom of the table.

## Records
| Timestamp | Task | Action | Result |
|-----------|------|--------|--------|
| 2025-10-31 | Implement ChatGPT button feature for log articles | Added "Ask ChatGPT" button next to existing "MD" button on individual log articles. Created URL encoding function to safely pass article title and URL to ChatGPT. Modified Astro component to include the new button with proper styling and link formatting. | Users can now discuss articles with ChatGPT through a one-click button that opens ChatGPT with pre-populated prompt including article title and URL for immediate conversation. |
| 2025-10-31 | Fix article header button spacing overlap | Addressed visual overlap issue where MD and ChatGPT buttons were colliding with long article titles that wrap to multiple lines. Added CSS modifications to create proper spacing between title text and action buttons. | Modified `.log-entry-header` to include `padding-top: 1rem;` and updated `h1` titles within the header to have `padding-right: 6rem;`. Applied mobile-responsive breakpoint at 768px to reduce padding to `1rem` on smaller screens. Result: Buttons now maintain consistent positioning without overlapping article titles regardless of title length or screen size. |
| 2025-10-31 | Restructure article header layout for better metadata organization | Moved publication date to be inline with MD and ChatGPT buttons for more professional header grouping. Created cohesive layout with publication date on left and action buttons on right. | Implemented new `.article-header-tools` container with flexbox layout. Separated publication meta from author meta into distinct containers. Removed absolute positioning from MD/ChatGPT buttons and eliminated padding requirements for title overlap. Added mobile stacking behavior with responsive breakpoints. Result: Clean, cohesive header with publication date and action buttons properly aligned while maintaining all existing functionality. |
| 2025-10-31 | Improve article header visual hierarchy with author meta repositioning | Moved author meta element to create cleaner visual flow in article headers. Enhanced reading order by placing author attribution after the main title. | Modified LogEntryLayout.astro to move author meta from between publication tools and h1 title to after h1. Updated CSS for author meta container by changing margin-left from var(--space-sm) to 0. Result: Cleaner visual hierarchy with author attribution appearing below the title, improving article header organization and creating more logical reading flow similar to traditional article layouts. |
| 2025-10-31 | Optimize publication meta vertical alignment | Changed vertical alignment of publication metadata to improve visual cohesion with action buttons. Publication date was previously center-aligned but needed better baseline alignment. | Modified CSS property in .article-header-tools container: changed `align-items: center` to `align-items: flex-end`. This single line change repositions the publication date to align with the bottom of the container rather than the center. | Publication date now aligns to bottom of container, creating better baseline alignment with action buttons. Result: Improved visual cohesion between publication metadata and action buttons with a more polished appearance where the publication date and buttons share a common baseline alignment. |
| 2025-10-31 | Improve mobile layout order for better user experience | Mobile layout had poor visual hierarchy with elements appearing in confusing order for users on small screens. | Changed mobile layout order using CSS flexbox modifications: Changed flex-direction from column to column-reverse in mobile media query (768px breakpoint), added order properties to control element positioning (article-actions: order: -1, publication-meta: order: 1), and maintained responsive design principles. | Mobile layout now flows as: [MD] [Ask ChatGPT] buttons (right-aligned) → Published on [date] → Article Title → Author attribution. Result: Cleaner, more intuitive mobile layout with better visual hierarchy where users first see action buttons, then publication date, then main title, creating a more logical mobile experience. |
| 2025-10-31 | Fix mobile layout order with explicit order properties | Previous mobile layout fix using column-reverse didn't work correctly, still showed date first instead of buttons. The flex-direction approach wasn't effectively reordering the elements as intended. | Implemented corrected approach using explicit order properties with standard column flexbox: Kept flex-direction: column, Set order: 1 for .article-actions (buttons come first), Set order: 2 for .publication-meta (date comes second). Applied changes in mobile media query at 768px breakpoint. | Final mobile layout flow: [MD] [Ask ChatGPT] buttons (right-aligned, order: 1) → Published on [date] (order: 2) → Article Title → Author attribution. Result: Successfully achieved desired mobile layout with buttons appearing first, then date, then title using explicit order control for more reliable element positioning. |
| 2025-10-31 | Fix critical E-E-A-T author link issues for better SEO and UX | Identified poor user experience and weak SEO signals from broken author linking where author links pointed to "#author" instead of "/bio", undermining site's E-E-A-T signals despite having excellent author credentials and structured data in place. | Implemented three key improvements: 1) Fixed main author link from href="#author" to href="/bio" in LogEntryLayout.astro, 2) Made author name clickable in author section linking to /bio, 3) Added Schema.org Person structured data with itemscope itemtype="https://schema.org/Person" and itemprop attributes throughout author sections. | Resolved critical E-E-A-T issue with proper author-content association and enhanced structured data. Users now experience consistent navigation behavior with clickable author attribution throughout articles. SEO benefits include stronger authorship signals, improved Experience, Expertise, Authoritativeness, and Trustworthiness metrics, and proper Schema.org markup for search engines. This fix leverages the site's existing excellent author credentials by ensuring they're properly discoverable and attributed. |
| 2025-10-31 | Complete final E-E-A-T enhancements for comprehensive author attribution | Added two final improvements to strengthen E-E-A-T signals: author names in article listings and expanded author bio with comprehensive credentials to ensure consistent author attribution across entire site. | 1) Modified LogItem component to display author name under date with new CSS classes (log-item-meta-left, log-item-author) linking to /bio page, 2) Enhanced article footer bio from single sentence to comprehensive multi-sentence bio including "14+ years experience", "€34M+ in EU funding", "Faculty at University of Split", expertise in "scaling innovation ecosystems", "digital transformation", "complex network systems", "AI adoption patterns", 3) Maintained Schema.org markup with itemprop="description". | Near-complete E-E-A-T implementation achieved. Experience: Consistent author attribution across all content listings (log index, tag pages). Expertise: Comprehensive credentials displayed in every article footer. Authoritativeness: Academic and professional achievements highlighted throughout site. Trustworthiness: Transparent, detailed author information on every page. Result: Strong E-E-A-T signals established across entire site with professional author credentials consistently presented to users and search engines. |
| 2025-10-31 | Final UI adjustments for author display based on user feedback | Implemented three key improvements to author presentation: enhanced bio with startup experience, streamlined log page format, and simplified article footer layout. | 1) Updated author bio from generic claims to specific startup experience: "Worked with innovative startups like Numarics, Codeanywhere, Daytona, and Steel on growth strategies and market positioning" while maintaining faculty research mention, 2) Changed log page author format from separate lines to inline format displaying as "October 28, 2025 - by Nikola Balić" by updating LogItem component to put author in same paragraph as date, 3) Removed author image from article footer by eliminating author-avatar div and img element while maintaining Schema.org structured data. | Cleaner, more focused author presentation with real-world startup experience credibility. The bio now provides concrete examples of work with specific startups rather than generic metrics. Log page layout is more streamlined with inline date-author format. Article footer is simplified without visual clutter from author image. All changes maintain SEO benefits while improving user experience and establishing stronger professional credibility through tangible startup experience. |
| 2026-03-06 | Analyze article voice from content log | Read representative articles across early, recent, technical, personal, and long-form narrative log entries (`vacuum`, `converted`, `2025-year-in-review`, `great-coding-agent`, `search-translator`, `cashout`, `proxmox-farm`, `agent-web`, `berghain`) and scanned the full folder for structural patterns such as headings, blockquotes, and list usage. | The recurring house voice is thesis-first, mechanism-driven, and conversational without fluff. Articles usually open with a strong claim, ground it in personal experience or observed evidence, expand into systems framing, then cash out into crisp bullets or a punchline. Reusable style markers: high-contrast headings, short emphatic paragraphs, strategic bolding, blockquotes for anchor ideas, and repeated focus on loops, traces, reliability, workflows, trust, and outcomes over vibes. |
| 2026-03-06 | Quote skill manifest descriptions that contain colons | The local skill loader skipped `article-writer` because its YAML frontmatter used an unquoted `description` value containing a colon in normal prose (`voice: conversational`), which breaks YAML parsing. | Wrapped the full `description` string in double quotes and escaped the embedded quotes. Verified the frontmatter parses cleanly with Ruby `YAML.safe_load` before resuming the build and git flow. | Skill manifests need stricter YAML hygiene than regular markdown. If a frontmatter value contains `:` in prose, quote it explicitly or the skill will be ignored at load time. |
| 2026-03-06 | Install Puppeteer Chrome before expecting OG prebuild output | The OG image prebuild script does not fail the site build when Chrome is missing. It logs a skip message instead, which can make it easy to assume images were generated when they were not. | Installed the browser runtime with `pnpm exec puppeteer browsers install chrome`, then reran `pnpm run build` so `scripts/prebuild-og.js` could complete the actual OG generation step. | Fresh OG assets depend on a local Puppeteer browser install, not just the Node dependency. If OG output is unexpectedly missing, install Chrome first and rerun the build. |
| 2026-03-06 | Strengthen thesis-driven essays with one live case study | Updated `post-copyright-era-software` by adding Cloudflare's `vinext` launch inside the existing argument instead of spinning out a separate news detour. | Used the example to illustrate both halves of the thesis: AI makes reimplementation dramatically cheaper, but trust and operational legitimacy remain scarce. | For conceptual software essays, the best current examples do not just prove acceleration. They should also expose what still does not get commoditized. |
| 2026-03-06 | Use adjacent expert commentary to widen the frame, not restate it | Added Mladen Vukmir's Platforum9 argument to the copyright essay as supporting evidence from an IP practitioner. | Placed the reference in the legal-framework section and used it to extend the piece from doctrinal instability toward the distribution of AI-created economic value. | The best supporting citations in essays do more than agree with the thesis. They should expand the axis of analysis. |
| 2026-03-06 | Personal references in essays need fast credibility, not full biography | Revised the Mladen Vukmir citation to foreground the personal relationship while keeping his relevance brief and specific. | Rewrote the paragraph as "My friend Mladen Vukmir..." followed by one compact credential sentence tied directly to IP and technology law. | When citing someone personally known to the author, the strongest pattern is: relationship first, one-line authority second, then the substantive point. |
| 2026-03-06 | Credential lines work best when they name institutions readers recognize | Refined the Mladen Vukmir citation again to add `ECTA` and `INTA` without expanding it into a bio. | Folded the organizations into the existing credibility sentence rather than adding a second explanatory line. | In essay citations, recognized institutional anchors often do more work than extra prose. |
| 2026-03-06 | External commentary can sharpen the significance of a case study | Added a short Gergely Orosz reference next to the `vinext` example in the copyright essay. | Used his framing to distinguish between the immediate product quality of `vinext` and the broader signal that large-scale reimplementation has become much cheaper. | A good outside citation does not need to repeat facts. It can clarify why the example matters. |
| 2026-03-06 | Structural compression works better than paragraph-by-paragraph trimming | Reworked `post-copyright-era-software` around earlier proof, merged sections, a faster thesis, and one steelman paragraph instead of making small local edits. | Moved the core formulation and `vinext` example to the front, merged adjacent sections, shortened the credential-heavy passages, and cut the draft from roughly 2.2k words to roughly 1.25k words while keeping the main evidence. | When an essay peaks too early, fix the order of proof first. Compression gets easier and the argument usually gets stronger. |
| 2026-03-06 | A renaissance label needs pressure before payoff | Adjusted the compressed copyright essay to add the selection-pressure paragraph before the conclusion and renamed the closing section to `The Software Renaissance`. | Expanded the abundance paragraph with why cheap implementation does not remove the difficulty of making good software, then reframed the ending around a more earned renaissance claim. | If an essay ends on flourishing, show the filter before the flourish. The abundance argument gets stronger when readers see why chaos does not win by default. |
| 2026-03-06 | Build public metadata URLs from `Astro.site`, not `Astro.url` | Production pages were emitting `localhost` in canonical URLs, Open Graph URLs, and structured data because `Astro.url` reflected the server runtime host rather than the public site origin. At the same time, a draft log entry was still directly routable through `src/pages/[...slug].astro`, so it pointed to a non-existent static OG image. | Added a shared site URL helper, switched metadata generation to `Astro.site` plus `pathname`, blocked draft log entries from the catch-all route outside local dev, and marked the published copyright article as non-draft so the OG prebuild would generate its image. | For Astro server deployments, treat `Astro.url` as request context, not canonical public origin. Public metadata should be derived from `Astro.site`, and draft accessibility rules need to match OG asset generation rules. |
| 2026-03-10 | Analogy-driven essays need one explicit break point and one lived behavior change | Revised `bubble-long-game.md` to soften over-absolute claims, acknowledge where AI already is changing work, compress the Petrarch section into a cleaner support for the thesis, and add one concrete paragraph about changed personal behavior. | The argument became more credible and more personal without changing the thesis: analogy supplies the frame, but the essay now admits present-tense exceptions and ends with a practical shift in attention rather than pure abstraction. | When an essay leans on a historical analogy, explicitly state what is already true now and cash the thesis out into one concrete behavior change. That keeps the piece from feeling over-argued or purely intellectual. |
| 2026-03-10 | Personal scene-setting in essays should be precise about place and time | Corrected the opening of `bubble-long-game.md` from implying residence in San Francisco to stating the actual timeframe spent there. | Rewrote the first sentence to say `Spending February and March 2025 in San Francisco...` so the credibility of the setup matches the facts. | In first-person essays, atmospheric setup still needs factual precision. Small biography shortcuts weaken trust faster than they strengthen the prose. |
| 2026-03-10 | Astro 6 requires the new content layer config shape and breaks old collection URL fields | Upgrading from Astro 5 to Astro 6 failed the build because the repo still used `src/content/config.ts`. Moved the file to `src/content.config.ts`, added explicit `glob()` loaders for each collection, and then replaced the old `entry.slug` usage with `entry.id` in pages, feeds, RSS, markdown endpoints, and redirect routes. Verified with repeated `pnpm run build` runs and by grepping generated output for `/undefined` links. | Astro 6 content collections do not tolerate the legacy config location, and old path code can silently degrade into broken prerendered links instead of hard failures. After upgrading, check both the build logs and the generated HTML for `/undefined` or empty route output. |
| 2026-03-10 | Prerender-safe middleware must short-circuit before reading request headers | Astro 6 kept warning that `Astro.request.headers` was used while prerendering `src/pages/tags/[tag].astro`, even though the page itself did not touch request state. I traced the warning into `src/middleware.ts` and found the markdown-negotiation middleware was reading `request.headers` before it returned early for `/tags/` and other static routes. Reordered the path allowlist so those routes exit before any header access, then reran `pnpm run build`. | On prerendered routes, middleware counts as part of the render path. If a route should bypass negotiation/auth/request-aware logic, put the path guard before any `request.headers`, `cookies`, or similar access or Astro will emit misleading page-level warnings. |
| 2026-03-10 | Thesis sections in essays benefit from one simple in-line diagram | Added `20260310_complements.jpg` to the `The Complements Are Where Value Accrues` section of `bubble-long-game.md` using the site's standard markdown image path format. | For concept-heavy essays, a single concrete diagram placed at the core thesis section can make the argument legible faster than another paragraph of explanation. |
| 2026-03-13 | Log article descriptions must respect the Astro content schema cap during drafting | Fixed `src/content/log/ai-native-dev-teams.md` after `pnpm run dev` failed with `InvalidContentEntryDataError` because the frontmatter `description` was 213 characters. Shortened the description to 105 characters and kept the rest of the frontmatter unchanged, then verified the site build again. | Log collection `description` fields are capped at 130 characters in `src/content.config.ts`. New draft articles can break the entire content sync if that limit is exceeded, so validate frontmatter lengths before assuming the markdown body is the problem. |
| 2026-03-13 | Thesis-heavy essays need rhythm changes, not just cleaner wording | Revised `ai-native-dev-teams.md` with a line-editing pass focused on cadence rather than argument changes. Tightened repeated formulations, alternated short and long sentences more deliberately, and used controlled repetition to reinforce the core idea without turning the prose monotonous. | When an essay is making one central claim over many sections, sentence variety does structural work. Repetition is strongest when it returns with slight shifts in meaning, and short sentences only feel sharp if longer sentences still carry some of the load. |
| 2026-03-13 | Dotfiles inside `src/content/log` still count as content entries and trigger OG generation | Generated LLM rewrite alternatives as hidden siblings under `src/content/log`, then discovered the Astro content glob and `scripts/prebuild-og.js` still picked them up as real log posts. Moved the alternatives out to a repo-root `.llm-alternatives/` directory and removed the accidental `.png` OG outputs before rerunning the build. | For this repo, draft alternatives and editorial experiments must live outside `src/content/log`, even if the filenames start with `.`. Hidden markdown files there are not safely ignored by the content or OG pipelines. |
| 2026-03-13 | Anthropic model names for `llm` must be verified locally before generating alternatives | Tried to generate new rewrite variants with `claude-sonnet-4.6` and `claude-opus-4.6` after a user request, but `llm models` only exposed Anthropic entries through `claude-sonnet-4.5` and `claude-opus-4.5`. Direct probes for `claude-sonnet-4.6`, `claude-opus-4.6`, `anthropic/claude-sonnet-4-6`, and `anthropic/claude-opus-4-6` all returned `Unknown model`. | For editorial comparison workflows in this repo, do not assume a newer Anthropic alias exists just because the user names it. Check `llm models` and then probe the exact model string before launching long rewrite jobs. |
| 2026-03-13 | Refreshing Anthropic models in `llm` required a plugin upgrade, not a core CLI upgrade | `llm` itself was already current at `0.28`, but the Anthropic registry was stale because `llm-anthropic` was still on `0.23`. Upgraded the active pyenv environment with `python -m pip install --upgrade llm llm-anthropic llm-openrouter llm-groq`, which actually changed `llm-anthropic` to `0.24` and `anthropic` to `0.84.0`; after that, `llm models` exposed `claude-sonnet-4.6` and `claude-opus-4.6` and a direct `llm -m claude-sonnet-4.6` call succeeded. | When `llm models` looks outdated, inspect plugin versions before assuming the core CLI is the problem. For Anthropic entries in this setup, model availability tracks the `llm-anthropic` plugin version. |
| 2026-03-13 | Rewrite alternatives need structural validation, not just style evaluation | Generated `claude-sonnet-4.6` and `claude-opus-4.6` alternatives for `ai-native-dev-teams.md` and spot-checked both the opening and the ending before comparing them. Sonnet 4.6 introduced a duplicated `## The Wrong Starting Point` heading and dropped the original opening section structure, while Opus 4.6 preserved the article shape more reliably. | For long-form editorial comparisons, do not judge outputs only by sentence quality. Verify headings, section order, and repeated argument beats before treating a variant as usable. |
| 2026-03-13 | Final essay drafts work best as synthesis, not model selection | Compared the canonical `ai-native-dev-teams.md` draft against `gpt-5.2`, `gpt-5`, `claude-sonnet-4.5`, `claude-sonnet-4.6`, and `claude-opus-4.6` alternatives, then merged the strongest lines into the real article and deleted the temporary variants. | For long-form essays in this repo, the winning workflow is: generate alternatives, extract a few stronger lines or section moves, then hand-synthesize the final piece. Even strong model drafts are usually too uneven to publish unchanged. |
| 2026-03-13 | Rough edit passes should cut setup before they cut proof | Did one more compression pass on `ai-native-dev-teams.md` after the synthesis draft still felt too cushioned. Removed section throat-clearing, duplicated rhetorical beats, and extra transition lines while keeping the core examples, lists, and thesis structure intact. | When an essay still feels fluffy after a normal line edit, the next move is not prettier wording. It is deleting setup, reducing commentary around examples, and forcing each paragraph to either prove the claim or move the argument forward. |
| 2026-03-13 | First-person essays should describe real provenance, not reconstructed source process | Revised `ai-native-dev-teams.md` again after the article still implied that the author had gone through transcripts or generic workshop materials. Replaced that language with direct phrasing about participating in workshops, writing up notes afterward, and quoting lines that were written down. | In first-person essays, source framing has to match what actually happened. If the author attended workshops and later wrote notes, say that plainly instead of implying transcript analysis or abstract source review. |
| 2026-03-13 | Narrative flow in thesis essays comes from sequencing, not more line polish | `ai-native-dev-teams.md` still read like stacked notes after multiple prose passes because the thesis landed too early, the middle repeated the same diagnosis across too many sections, and the concrete operating picture arrived late. | Rebuilt the article around six sections instead of eleven, moved the operational example layer earlier, merged overlapping diagnosis sections, and shortened the body while keeping the thesis intact. | When a short essay feels smart but static, stop polishing sentences and change the shape. Fewer section resets, earlier concrete payoff, and a clearer cause-and-effect arc do more for flow than another round of line edits. |
| 2026-03-21 | YAML question strings in log frontmatter must be quoted when they contain colons | `pnpm run build` failed on `src/content/log/optimizing-skills.md` because an `answers_questions` entry used `What matters more for agent performance: ...` without quotes, so YAML parsed it as an object instead of a string. Quoted all `answers_questions` lines and reran the build. | In this repo's log collection, frontmatter list items under `answers_questions` should be quoted defensively, especially when they contain `:`. Otherwise Astro content validation fails before the site build can proceed. |
| 2026-03-29 | When adding a new log article, tune the draft against live house style before saving | Read representative AI/agent workflow posts from `src/content/log/` (`great-coding-agent`, `eager-agents`, `cashout`, `theloop`) plus a recent site scrape to match the thesis-first, mechanism-driven tone before drafting `oracles.md`. | For this site, the best article drafts open with a sharp claim, compress the thesis into a few bullets early, and use short sections to cash out the mechanism. Matching that structure matters as much as matching the topic. |
| 2026-03-29 | Humanizing a draft for this site means breaking the "framework voice" before polishing sentences | Rewrote `src/content/log/oracles.md` to remove chant-like bullet ladders, repetitive "That is..." cadence, and abstract AI-workflow slogans. Replaced them with more opinionated prose, a few sharper judgments, and paragraph shapes closer to nearby posts like `eager-agents.md`. | On nibzard, text can still sound machine-written even when the sentences are clean. The fix is not just word substitution; it is reducing list density, varying rhythm, and letting the writer sound like they actually have a stake in the argument. |
| 2026-03-29 | Interlinks work best when they signal continuity, not self-citation | Revised `src/content/log/oracles.md` to link earlier loop/agent essays exactly where the argument overlaps: `/theloop` for the prior thesis, `/eager-agents` for hard boundaries, and `/great-coding-agent` for the harness framing. | On this site, internal links land better when they tell the reader "this is the earlier piece of the same idea" instead of being tacked on as related-reading ornaments. |
| 2026-03-31 | Guide-style source material needs one controlling thesis before it reads like a publishable essay | Drafted `src/content/log/wrappers.md` from a long practical guide about autonomous agent wrappers. Instead of preserving the original 20-point checklist shape, rewrote it around one thesis, "Bash owns the loop," then kept only the code snippets and lists that strengthened that core idea. | When turning internal notes or implementation guides into a log article, start by naming the governing sentence first. Once the thesis is sharp, the checklist can stay, but only as support instead of structure. |
| 2026-03-31 | Humanizing technical essays usually means deleting scaffolding, not adding flair | Revised `src/content/log/wrappers.md` with the `humanizer` skill after the first draft still had too many clean bridges like `That is`, `This is`, and repeated subject lines like `The wrapper...`. Kept the thesis and code examples, but roughened the transitions, made the voice more first-person, and let a few sentences land without explanatory padding. | In this repo's essay style, the obvious AI tell is often not wild vocabulary but over-assembled structure. If a draft sounds too neat, cut the connective tissue first and let the argument breathe. |
| 2026-03-31 | Humanizing workshop-based essays works best when you rough up the transitions, not the thesis | Revised `src/content/log/ai-native-dev-teams.md` after the argument was already solid but the prose still felt over-assembled. Kept the core claims about legibility, structure, and human roles, then rewrote section openings, added more first-person phrasing, lowered heading polish, and replaced a few tidy contrast pairs with messier spoken transitions. | For strong thesis essays in this repo, the fastest way to make a draft sound human is not to weaken the claim. Keep the spine, then remove the over-clean bridges and let the paragraphs sound like someone actually thinking on the page. |
| 2026-03-31 | Astro Vercel builds can emit noisy Node builtin externalization warnings and trigger `.vercel/output` watch spam in dev | Investigated `pnpm run build` and `pnpm run dev` logs after the site built successfully but printed many `[vite] Module "... has been externalized for browser compatibility` warnings from `@astrojs/vercel`, `@vercel/*`, and `esbuild`, followed by many `[watch] .vercel/output/static/...` lines in dev. Confirmed the repo is using `output: 'server'` with the Vercel adapter and that the warnings originate from adapter/tooling internals rather than site code. | In this repo, those warnings are usually benign if the build completes: Vite is externalizing Node builtins used by server-side adapter code, not reporting a client bundle bug. The later `[watch]` flood comes from dev mode noticing generated files under `.vercel/output` after a build. If the noise becomes distracting, ignore `.vercel/output/**` in Vite dev watch settings rather than chasing the adapter warnings as an application error. |
| 2026-04-16 | TinaCMS init scaffolding does not map cleanly onto this Astro content setup | Investigated a Tina init run that added `content/posts/hello-world.md`, `tina/config.ts`, and `public/admin/` to a repo whose actual editable content already lives in Astro collections under `src/content/{log,now,thoughts,images,idea}`. Replaced the demo Tina collection with repo-specific collections, added a `pnpm tinacms:dev` script that runs Tina alongside `pnpm dev`, and removed the unused hello-world post. | For this site, Tina should be configured against the real Astro collection paths, not the default `content/posts` demo. Running `tinacms dev` without the site command is misleading here; use `pnpm tinacms:dev` so Tina has a real dev server to attach to. |
| 2026-04-16 | Mixed Markdown and MDX content needs explicit Tina boundaries | While validating Tina against `src/content/log`, found that Tina exposes the collection as `md` and rejects the repo's `agent-stress-test.mdx` entry with `Invalid file extension: expected '.md' but got '.mdx'`. That file also imports an Astro component, so it cannot be safely edited as plain markdown. Excluded the MDX article from the Tina log collection for now and removed the generated `public/admin/.gitignore` so the admin entrypoint is not accidentally ignored by git. | In this repo, Tina should only manage markdown collections unless MDX files are modeled explicitly with Tina templates. If a collection mixes `.md` and `.mdx`, either split it or exclude the MDX entries; otherwise the CMS will boot but silently fail on those documents. |
| 2026-04-16 | Tina admin must be generated during the site build or `/admin` 404s in production | Confirmed `pnpm exec tinacms build --local --skip-cloud-checks` writes `public/admin/index.html`, while `tinacms dev` only helps locally. The production 404 happened because the build pipeline was not generating Tina's static admin page before Astro/Vercel deployment. | For Tina on Vercel, `pnpm build` must run Tina first so `/admin` ships with the deploy. A generated admin page sitting only in a local worktree is not enough. |
| 2026-05-14 | Design-system overrides must account for Astro CSS and critical CSS ordering | While aligning the site with `DESIGN.md`, old duplicated rules in `global.css` and generic critical CSS selectors overrode the newer modular styles, causing flex instead of grid, square cards, dark-mode shadows, tag-colored rails, clipped mobile cards, and boxed nav backgrounds. | Added a final design-system layer at the end of `global.css`, moved the critical nav background to transparent, removed tag-derived rail styling from home cards, and verified with `agent-browser` at desktop and 320px mobile plus `pnpm run build`. | In this repo, import order alone is not enough because critical inline CSS and legacy global rules can still win. When changing foundational styles, verify computed styles in-browser for grid display, card width, radius, shadow, rail color, and theme surfaces. |
| 2026-05-14 | Final design-layer rules must preserve responsive overrides | After moving the theme toggle into the desktop nav and adding final-layer nav alignment rules, mobile rendered both the full `.site-nav` and hamburger because the final `.site-nav { display: flex; }` appeared after the earlier mobile `display: none` breakpoint. | Added a final-layer `@media (max-width: 768px)` rule that hides `.site-nav`, shows `.mobile-menu-trigger`, and restores the row header layout. Verified with `agent-browser` at 390px that desktop nav is hidden and hamburger is visible, then ran `pnpm run build`. | Any selector re-declared in the final design-system layer needs its responsive exceptions repeated there or moved after it. Otherwise the final layer can silently undo mobile behavior even when the original breakpoint still exists. |
| 2026-06-10 | Cross-posted articles need an external canonical wired through every content surface, not just the HTML head | Added `deep-research.md` as a copy of a steel.dev original. Introduced an optional `canonical` field in the log schema (`src/content.config.ts`), threaded it `LogEntryLayout` → `BaseLayout` (new `canonical` prop overriding the self-derived URL), and also honored it in the two markdown-negotiation paths (`src/middleware.ts` log branch and `src/pages/api/raw/[slug].ts`) so the `Link: rel="canonical"` header and `canonical-url:` agent metadata point at the original too. | For SEO correctness on a cross-post, the HTML `<link rel="canonical">` is necessary but not sufficient: this site also serves markdown via content negotiation, and both the HTTP `Link` header and the embedded agent metadata default to a self-canonical. Set the canonical in every surface that emits one, and verify with `curl -H "Accept: text/markdown"` plus a `.md` URL, not just the rendered page. |
| 2026-06-10 | Theme-aware article body images use the manual `[data-theme]` attribute, not `prefers-color-scheme` | The deep-research illustrations ship as light + dark PNG pairs. The site default theme is dark, so light-only images would be white boxes. Added two `<img>` tags per figure (`class="img-light"` / `class="img-dark"`) with a CSS block in `global.css` that toggles `display` off `[data-theme="light"]`. | The site theme is a manual `data-theme` attribute toggle that can disagree with the OS preference, so a `<picture>` + `prefers-color-scheme` swap would desync from the toggle. Match the existing `[data-theme="..."]` selector convention instead. Raw HTML `<img>` works fine inside markdown articles, and `article img` already styles them. |
| 2026-07-23 | pnpm 11 stopped reading `pnpm.onlyBuiltDependencies` from package.json, breaking `pnpm run build` before Astro even started | Every `pnpm run build` failed in pnpm's pre-run deps check with `ERR_PNPM_IGNORED_BUILDS` because the approved-builds list moved: pnpm 11 only reads `allowBuilds` in `pnpm-workspace.yaml`, and that file contained unfilled `set this to true or false` placeholders. Filled in `allowBuilds` mirroring the old config (`better-sqlite3: true`, everything else false). | pnpm treats an unfilled `allowBuilds` placeholder as an error, not a warning. If builds fail before any Astro output with an ignored-build-scripts message, fix `pnpm-workspace.yaml` — package.json's `pnpm` field is dead config now. |
| 2026-07-23 | better-sqlite3@11 (via TinaCMS search) cannot build under Node 26; build with Node 24 instead | With build scripts approved, install still failed: better-sqlite3@11.10.0 has no Node 26 prebuilt binary and its source no longer compiles against Node 26's V8 (`GetPrototype` removed, fixed upstream in v12). The dep is transitive via `tinacms → @tinacms/search → sqlite-level`, and `tinacms search-index` runs in every build. Worked around it by prefixing the build with `export PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH"`, which lets prebuild-install fetch a prebuilt binary. | Until TinaCMS pulls better-sqlite3 v12 (or a pnpm override forces it), site builds require Node ≤24. Consider adding an `.nvmrc` or the override as a durable fix. |
| 2026-07-24 | Run a multi-agent deep-research pass over draft log articles before publishing | Fanned out one three-stage Workflow per draft (`christmas-moment`, `stopgap`, `beside-you`, `packaging`, `injected`, `farming`): one agent decomposed each draft into 5–8 research facets, then one web-research agent per facet ran 3–6 searches plus primary-source fetches returning structured verdicts (`confirmed`/`refuted`/`mixed`/`unverifiable`) with source URLs and dates, then a synthesizer folded the findings back over the draft into a brief (claims table, corrections, strengthening opportunities, counter-evidence, source library). Wrote outputs to `/research/<name>-research.md` — outside `src/content/log`, since the content glob (`**/*.{md,mdx}`) and `prebuild-og.js` would otherwise treat them as real articles (see the 2026-03-13 dotfiles lesson). | 55 agents, ~2.6M tokens, ~32 min, ~24k words across 6 docs. Already surfaced load-bearing errors: the `christmas-moment` hook ("nothing changed in December") is refuted by Opus 4.5 (Nov 24 2025) and GPT-5.2 (Dec 11 2025); "clueso" is a different company; the `injected` piece's "streams results back over CDP" is architecturally backwards and the "$17M Series A" is uncorroborated. The prompt design that made it work: explicitly separate web-verifiable facts from the author's personal anecdotes so researchers don't chase private experiences, and instruct them to treat negative findings (an event didn't happen) as valuable rather than as failures. Workflow synth agents sometimes HTML-escape `>`/`<` in markdown, so `sed 's/&gt;/>/g; s/&lt;/</g'` is needed when writing their output to `.md`. |
| 2026-07-24 | Extract structured returns from a completed Workflow run | A completed-workflow output file is not the script's bare return value; it is wrapped as `{ summary, agentCount, logs, result, workflowProgress, totalTokens, totalToolCalls }` with the return value at `.result` (a parsed object, not a string). Used `jq '.result.articles[]'` to enumerate articles and `jq -r '.result.articles[i].markdown'` to write properly-unescaped `.md` files. | `null | length` returns `0` in jq, so a wrong path looks like an empty array rather than erroring — verify with `type`/`keys` before iterating. Also: 1 of 55 agents hit a 429 rate limit on a web-research facet, and the parent article still synthesized from its remaining facets, so the decompose→research→synthesize pipeline degrades gracefully when a single facet fails. |
| 2026-07-31 | A failed `pnpm rebuild better-sqlite3` under Node 26 deletes the working Node 24 binary | Ran `pnpm rebuild better-sqlite3` under Node 26 to fix the known NODE_MODULE_VERSION mismatch (see 2026-07-23 lesson); the gyp compile failed as expected, but the failed rebuild also removed the previously working compiled binary, so subsequent Node 24 builds failed too with "Could not locate the bindings file". Recovered by rerunning `pnpm rebuild better-sqlite3` under Node 24 (`. "$NVM_DIR/nvm.sh" && nvm use 24`), which compiled cleanly. | Never attempt the rebuild under Node 26 — it makes things worse, not just fails. When the search-index step breaks, switch to Node 24 first, then rebuild if needed. The full build (`nvm use 24 && pnpm run build`) passes end to end afterward. |
| 2026-07-24 | Revise a batch of related drafts with a write+edit workflow, then run a cross-article consistency pass | Ran a second Workflow over the six drafts: per article a writer expanded the outline into full prose folding in its `research/<name>-research.md`, then an editor adversarially re-verified every URL/quote/number against the brief and returned final markdown plus a changelog. Outputs staged to `research/revised/<name>.md`; originals left untouched because they are untracked, so overwriting would destroy the only copy. | 12 agents, ~10 min, six 1.5–2.4k-word articles, all within frontmatter limits, all facts traceable to the research. Reusable gotchas: (1) fan-out makes each article self-consistent but the BATCH inconsistent — the Steel self-disclosure came out three different ways across the same-day set, so a related batch needs a final cross-article pass or a shared facts-sheet handed to every writer; (2) agents reliably HTML-escape `<`/`>` in markdown output, including the real `<blockquote class="featured-quote primary">` pattern used by ~38 published articles — `sed 's/&lt;/</g; s/&gt;/>/g'` restores it on write; (3) the log frontmatter key is `answers_questions` (plural) — a singular variant is silently dropped by the schema. |
| 2026-08-24 | Bypass the obsolete global Corepack shim when building under Node 24 | The required Node 24 build still failed with `ERR_VM_DYNAMIC_IMPORT_CALLBACK_MISSING` because `/usr/local/bin/pnpm` pointed to Corepack 0.18.0. Used the standalone pnpm 11 launcher in `/Users/nikola/Library/pnpm/bin` and placed that directory first for child commands invoked by the build script. | Switching Node versions is not enough when the global `pnpm` command resolves through the old Corepack install. For a reliable local build, use Node 24 and ensure `/Users/nikola/Library/pnpm/bin` precedes `/usr/local/bin`; the full TinaCMS, Astro, OG, and search-index build then passes. |
| 2026-08-24 | Sweep drafts for negation-first parallelisms before publishing | Audited `shapes.md` with `rg "isn't|is not|not because|rather than|, not |never|does not|didn't|cannot"` and found 9 instances of the "isn't X, it's Y" construct family (`not X, but Y`, `X, not Y`). Rewrote 7 by stating the positive claim directly (usually the negation pivot just deletes: "what remains isn't a computer at all. It's closer to a body" → "what remains is closer to a body"). | The construct is a machine-writing tell when reflexive, but it is also how punchlines work. The rule that held: rewrite every instance where the positive statement loses nothing, keep at most one or two where the two-beat snap IS the effect (the featured quote and the final line). Sweep with the grep above, then judge each hit on whether the negation carries information. Corollary from author feedback: when replacing a tic, do not compress into metaphor-crypto ("The form field hid a constraint" read as vague and confusing) — if the surrounding grafs already carry the point, delete the sentence instead of rewriting it. |
| 2026-08-24 | Own projects build credibility as instruments in a scene, not as announcements | Reframed the AgentLab paragraph in `shapes.md`: "I wrote AgentLab." led the graf and read like product PR even with an undercut sentence after it. Rewrote so the observed nightly work stays the subject and the tool appears in apposition: "Most of that work runs through AgentLab, a tool I wrote to run coding agents in dangerous mode." | When mentioning your own projects for credibility, name them as the instrument the described work runs through, and let confessions (dangerous mode, what the tool is not) ride in subordinate clauses. The Proxmox paragraph is the model: observation first, no authorship headline. Keep first-person only where the claim is confession-shaped ("I built the cheap half" before AWC's limitations). Related: observations derived from team/company work take "we", not "I" — singular quietly claims solo credit for collective data. |
| 2026-08-24 | A dek's second beat should name the replacement, not the absence | Reworked the `shapes.md` description from "Every part of a computer is furniture arranged for a person. The person has left the room." to "Computers are rooms furnished for a person. The agents crashed anyway." The original stopped one beat short: it described an empty room when the thesis is occupancy, and singular "agent" would have echoed the one-machine-one-operator model the piece dismantles. | Pick a dek image the body already sustains (furniture/room recurs in intro and closer here), then make the second beat assert the new state with a plural subject. Pun-verbs like "crashed" (couch-crash + computer-crash) work when both senses are thesis-true and the audience is technical; ground any "anyway"-style shrug in a documented scene ("work that I didn't start"). |
| 2026-08-24 | Fetch AXINBOX preview annotations through the page wrapper, not the raw API | The author left review feedback as annotations on the shared `shapes.md` preview. `GET /v1/drops/:id/annotations?path=...` returns 401 (annotation capability missing), but the preview wrapper exposes `window.__AX_WRAPPER__.annotations.listEndpoint` with an embedded `acap` capability — fetch that from page context to get full annotation objects with anchors (`quote.exact/prefix/suffix`, char positions, CSS container). Only list/create/presence endpoints exist for viewers; there is no resolve endpoint, so handled feedback is dismissed in the UI by the author. | Annotation anchors pin to the artifact version at creation time; after re-uploading an edited HTML, applied feedback's anchor text no longer matches. Read annotations before generating the next version, apply, re-upload, then tell the author to dismiss the stale ones. |

| 2026-09-09 | Investigate Vercel Fast Origin Transfer limit | Inspected the Hobby team and live nibzard deployment with Vercel CLI, sampled production GET responses, and deduplicated runtime logs by request id. Used a temporary Vercel CLI 59.13.1 invocation because installed 50.31.1 lacks usage/metrics commands. | Nibzard is a strong suspect, but its share of the team total is unconfirmed: billing usage returned Costs not found (404), and project-grouped metrics required Observability Plus. Live homepage, log, search, article, and RSS responses were cache MISS; RSS was 1,632,003 bytes decoded / 355,603 bytes gzip and appeared in 21 of 50 distinct recent requests (200 CLI rows repeated those ids four times). Astro defaults to server output; RSS has no prerender opt-in. Prioritize prerendering RSS and suitable content routes, then safe CDN caching for remaining dynamic public routes. Preserve query pagination and HTML/Markdown content negotiation when changing caching. Static llms-full.txt was already a cache HIT. The live site remained HTTP 200; no production changes made. |
| 2026-09-09 | Verify Cloudflare protection against Vercel origin transfer | Repeated live GETs for homepage, RSS, and llms-full.txt and compared both CDN cache headers against Cloudflare documentation. | All three returned cf-cache-status: DYNAMIC on both requests. Homepage/RSS also returned x-vercel-cache: MISS and max-age=0, must-revalidate; llms-full.txt remained a Vercel HIT despite Cloudflare DYNAMIC. Proxying through Cloudflare does not imply HTML/feed caching. Inspect both cache layers; use targeted Cache Rules and suitable edge TTLs, preserving content negotiation, or prerender suitable routes at Vercel. |
| 2026-09-09 | Identify RSS as the main transfer driver using Cloudflare analytics | Used the project-local Wrangler OAuth login for zone lookup and read-only GraphQL analytics. Queried daily totals for Aug 10–Sep 8 and Sep 8 adaptive route, cache-status, and user-agent groups. Wrote docs/origin-transfer-plan.md. | Analytics works with the existing OAuth token even though Cache Rules/Page Rules/settings return 403. Cloudflare reported 18.54 GB over 30 days with 7.6% of bytes cached. On Sep 8 RSS represented 499.8 MB of 601.6 MB (83.1%), across 1,317 requests, all dynamic; user agents include common feed readers. These edge metrics are not exact Vercel FOT accounting and adaptive groups may be sampled. Prioritize static RSS generation on Vercel before any wider refactor; optionally add a narrow Cloudflare feed cache. Current Cloudflare docs support configurable Vary on all plans, but application variation must consistently include Accept, User-Agent, and Sec-Fetch-Dest. |
| 2026-09-09 | Build the RSS fix in an isolated checkout without publishing local drafts | Added prerender=true to RSS and a route-specific five-minute browser / one-hour shared cache header. Ran the full build under Node 24 using existing dependencies and pnpm_config_verify_deps_before_run=false to prevent pnpm 11 from trying to reinstall a symlinked node_modules. Removed that symlink before Vercel source upload. | Build passed; .vercel/output/static/rss.xml contains exactly the same 81 items as the production baseline, with unchanged item content, and the RSS function route is absent. Isolated source upload excludes unrelated local drafts and dependency symlinks. |
| 2026-09-09 | Verify static RSS through both CDN layers before promotion | Deployed an isolated production candidate with --skip-domain, verified identical RSS entries, cache HIT, and conditional 304, then promoted it and repeated checks on www.nibzard.com. | Live RSS now returns Vercel HIT and conditional 304 while Cloudflare still reports DYNAMIC. Cloudflare forwarding no longer means RSS invokes the rendering function because the feed is a static build artifact. Candidate HTML/Markdown and pagination checks passed. Preserve deployment-source isolation when the main checkout has unpublished drafts. |
| 2026-09-09 | Prerender public pages and cache negotiated content safely | Prerendered about, bio, cv, projects, log, tags, and thoughts indexes; bypassed request-time middleware during prerendering. Added a Vercel-only one-hour content cache with identical sorted Accept, Accept-Encoding, Sec-Fetch-Dest, and User-Agent variation on HTML and Markdown. Added five cache-policy tests and checked real cache behavior with mixed browser/agent requests. | Local full builds and the production candidate passed. Auth, errors, mutations, Set-Cookie, private policies, and images retain safe handling; homepage pagination, search, and newsletter routes remain dynamic. Live Cloudflare checks revealed inconsistent variation lists between HTML and Markdown and prompted a regression test and normalization before finalizing. Cloudflare can strip HTML Vary while reporting DYNAMIC; downstream CDN no-store keeps format caching confined to Vercel. |

| 2026-09-09 | Write the GLM-5.3 Claude Code setup guide | Read both existing Z.ai articles and documented the verified 2.1.266 configuration in claude-glm.md. Validated shell snippets, JSON, and frontmatter lengths; the full build passed with Node 24 and a temporary pnpm launcher bypassing the older Corepack shim. | Separate the unknown-model warning from the API failure: behavesAs handles the catalog; the gateway rejected the Artifact tool schema. Verify interactive tool use because print mode passed while interactive mode failed. Scope the workaround and 1M context setting to the provider wrapper. |
| 2026-09-09 | Fix the search-index build failure caused by a poisoned Homebrew node@24 keg | Hit the known better-sqlite3 failure again (see 2026-07-23 and 2026-07-31) and traced the local cause: `/opt/homebrew/opt/node@24` is a symlink into `../Cellar/node/26.4.0`, so prefixing PATH with the keg silently runs Node 26. Rebuilt the module and ran the full build with mise's Node at `~/.local/share/mise/installs/node/24.19.0/bin` prefixed on PATH. | The mise path needs no nvm sourcing and sidesteps the stale Corepack shim (see 2026-08-24), so `pnpm run build` passes end to end. Durable fixes still open: `brew reinstall node@24` to repair the keg, or an `.nvmrc`/pnpm override for better-sqlite3 v12. |

<<<<<<< HEAD
| 2026-09-11 | Review rendered design before proposing a new visual system | Compared the live homepage and article with tokens and later global style overrides. Homepage headings render as uppercase monospace, while article titles use Tobias. | Token declarations alone do not describe the rendered hierarchy. Inspect both page types and mobile layouts. The installed impeccable context script is under /home/agent/.agents/skills, rather than this repository's local skills directory. |

| 2026-09-11 | Apply the workbench system across all public pages | Replaced the loaded legacy style stack with one shared stylesheet, moved CV into the shared layout, and kept archive entries reachable with native links. Added routes for older Now entries and image notes. | Check secondary routes as well as the homepage. Content wrappers must render visibly before hydration, and long search captions need overflow wrapping on mobile. |
| 2026-09-11 | Build a preview on the shared Linux machine | Installed the locked dependencies; better-sqlite3 compiled successfully under Node 24. Other services occupied Tina ports 4001 and 9000. The site build passed, but the cloud search upload lacked credentials. | Use TINA_PORT=4401 and TINA_DATALAYER_PORT=9401 for this checkout. TINA_SKIP_SEARCH_INDEX=1 explicitly skips the remote search upload; normal builds still run it. Avoid concurrent builds and browser audits against the same dev cache. |
| 2026-09-11 | Keep visual checks separate from the production build | Running Tina/Astro builds while auditing the development server triggered reloads and transient React runtime errors. | Stop the development server for the build, then restart it before browser checks. Keep browser tests focused on a stable server. |
| 2026-09-11 | Verify the full workbench rollout | Browser checks passed for 21 routes at desktop and mobile widths, plus keyboard navigation, theme persistence, search, article tools, Markdown access, simulated subscription feedback, and no-script rendering. | A secondary MDX subscription component needed its own label, focus treatment, and theme colors. Include custom article components in site-wide design reviews. |
| 2026-09-11 | Keep the Tailscale preview running between turns | A detached nohup command did not survive the command runner. Started the Astro preview with a systemd service running as agent and pointed the private Tailscale port 8443 at localhost:4321. | The service is nibzard-web-preview. Stop it before builds, then restart it. HTTPS checks passed for the homepage, article, projects, and image asset. |
| 2026-09-11 | Plan the About, Bio, and CV consolidation | The three pages repeat positioning and achievements. The CV compresses earlier work into a paragraph, omits Steel, and conflicts with Bio on some metrics and research descriptions. Author metadata and article links also point to /bio. | Give About a concise personal role and CV a complete professional record. Reconcile dates and claims before rewriting the CV, and preserve /bio links through a redirect when retiring that page. |

- **2026-09-11: Consolidate personal records with source checks.** Keep the About introduction, formal CV, and author metadata on shared career data. Institutional report title pages can contain authors omitted from profile listings. Use report dates rather than upload-path dates. Keep missing employment and degree dates in the source ledger instead of inventing a continuous timeline.
- **2026-09-11: Retire Bio with an HTTP redirect.** Use a non-prerendered Astro route for the 301 response. Update author links and exclude the old URL from the sitemap. Verify both trailing-slash variants and print styles under a dark appearance.
- **2026-09-11: Restart transient preview services explicitly.** Stopping `nibzard-web-preview.service` removes its transient unit. Recreate it with `systemd-run`, then confirm an HTTP response before browser tests. A started process is not yet a ready server.
- **2026-09-11: Reset the print color scheme.** A white body still leaves dark PDF margins when the root uses a dark color scheme. Set the root background to white and `color-scheme: light` in the CV print rules. Inspect the generated PDF, not only computed body styles.
- **2026-09-11: Check PDF text extraction.** Saans looks correct in the browser, but the exported CV title interleaves with nearby text in `pdftotext` output. Use Arial in print styles. The exported name and paragraphs then extract in the correct order, while the website keeps Saans.

- **2026-09-11: Separate GitHub metadata from editorial selection.** `src/config/projects.json` controls visibility and features. `src/data/github-projects.json` is the saved public snapshot. Both the directory and homepage use one selector. A failed refresh keeps the last valid snapshot; strict automation stops instead of committing partial results.
- **2026-09-11: Resolve renamed project keys during migration.** GitHub redirects the old `daytonaio/ai-enablement-stack` and `nibzard/skills-marketplace` names to `daytona/ai-enablement-stack` and `nibzard/skills`. Use returned canonical names in the sidecar. An old name must not silently publish a newly discovered project.
- **2026-09-11: Curate features beyond repository metadata.** Steel Experiments contains relevant work absent from the personal-account sync. Read README status and migration notices: Pi Steel moved to `steel-dev/pi-steel`, while Weave and Tamagotchip describe different stages of completion. Public contribution history helps establish involvement without assuming sole authorship of organization projects.

- **2026-09-11: Discover organization projects without publishing them.** Paginate each configured organization using public repository discovery, deduplicate by canonical name, and apply the same editorial visibility rules. Any source failure rejects the entire refresh so the saved snapshot remains complete. Use documented command examples when a new feature has no verified screenshot.

- **2026-09-11: Sync the Giscus theme with the site toggle.** `theme="preferred_color_scheme"` makes the Giscus iframe follow the OS `prefers-color-scheme`, not the `data-theme` attribute the site toggle writes, so comments stayed dark while the page was light. Giscus runs in a cross-origin iframe and cannot see `data-theme` or `localStorage`. Mirror the attribute with a `MutationObserver` in `GiscusComments.jsx`; `@giscus/react` v3 re-themes the iframe through a `setConfig` postMessage when the `theme` prop changes. `astro build` passes with the fix, and browser checks confirmed the widget follows light/dark toggles.

=======
| 2026-09-11 | Treat graph-density corrections as substantive editorial changes | Checked the working diff of `fre-production.md`: the conclusion changes dense to large sparse, and the featured quote changes dense to sparse. Both agree with the existing TLDR, sparse-case benchmark description, and explicit statement that dense graphs favor Dijkstra. | Record this as one claim correction in two places, separately from phrasing edits. Internal consistency confirms the intended graph regime; it does not independently validate benchmark measurements or establish an advantage for all sparse graphs. |
>>>>>>> b18f7f7 (edit: humanize all log articles)
