# Internal link inventory — nibzard.com

> Auto-generated by `scripts/link_audit.py`. Last updated: 2026-05-13.
> Pages scanned: 172. Total internal links: 126.

## How to use

Pillar pages (Phase 4) should each:
1. **Receive 3+ inbound links** from existing log posts that touch the cluster topic
2. **Send 5+ outbound links** to relevant existing log posts (clusters reference the pillar)

Anchor text rules: vary it. The same target should be linked with 3-5 different anchor variations across the site.

---

## Linkable URLs by category

### Hub / commercial-intent pages

| URL | Title | Suggested anchors |
|-----|-------|-------------------|
| `/` | nibzard - Home | "the home page", "nibzard.com" |
| `/about` | About - nibzard | "about Niko", "the about page", "Niko Balić" |
| `/bio` | Nikola Balić Official Bio | "official bio", "professional biography" |
| `/cv` | CV | "cv", "background", "credentials" |
| `/projects` | Projects | "projects", "what I've built", "shipped projects" |

### Content collections (where the SEO weight lives)

| URL pattern | Source | Notes |
|---|---|---|
| `/log/*` | `src/content/log/` | Main blog — pillar candidates live here |
| `/thoughts/*` | `src/content/thoughts/` | Short-form — supporting links into log |
| `/now/*` | `src/content/now/` | Now-page entries |
| `/idea/*` | `src/content/idea/` | Product/service ideas |
| `/tags/*` | auto-generated | Tag hubs — useful for topical authority |

### Auxiliary

| URL | Title | Suggested anchors |
|-----|-------|-------------------|
| `/search` | Search | "search", "find" |
| `/now.astro` | Now | "what I'm working on now", "current focus" |
| `/llms.txt` / `/llms-full.txt` | LLM-readable index | (special, AI-readable) |

---

## Issues surfaced by audit

### Broken internal links (101 detected — real issue)

The audit caught real broken slugs across log posts:

- `/log/ai-discovery` → links to `/agent-web` (does the page exist as `/log/agent-web`?)
- `/log/hn-hug` → multiple image paths missing (e.g. `/images/20260121-cloudflare-stats.jpg`)
- `/log/architecture` → links to `/some-article` (placeholder text that shipped)
- `/log/agent-web` → links to `/agent-labs` (slug mismatch?)
- `/log/demig` → links to `/claude-zhipu` (slug?)

**These are likely (a) slug mismatches where Niko renamed a post, (b) image paths that point to images that were never added to public/, (c) leftover placeholder text.**

**Recommended action:** run a one-off cleanup pass:

```bash
python3 /Users/nikola/.claude/skills/seo-sprint/scripts/link_audit.py /Users/nikola/dev/nibzard-web --base-host nibzard.com --broken-only
```

Then fix each broken link manually — either correct the target slug, or remove the link. These are SEO-meaningful: every 404 a crawler hits wastes crawl budget and signals neglect.

### Orphan pages (163 reported — mostly false positives)

The audit's URL mapping treats every `.astro` file as a routable URL, which is wrong for component / layout files. **Most "orphans" reported are not real orphans** — they're component files that wouldn't render as pages.

The script needs a fix: only treat files in `src/pages/` as routable pages, ignore `src/components/`, `src/layouts/`, etc. This is a real script limitation surfaced by dogfooding.

For now, **ignore the orphan list from this run.** Manual orphan check: any post in `src/content/log/` that isn't linked from `/log/index` (auto-generated) or from another post is at risk.

### Over-linked pages

None detected. Site link distribution is healthy.

---

## Anchor text variation seed list

For the 5 highest-value targets, vary anchor across posts:

- `/log/agentic-ai-handbook` → "Agentic AI Handbook", "the 113 patterns handbook", "battle-tested agent patterns", "the agentic AI book"
- `/projects` → "projects", "what I've shipped", "my recent builds"
- `/about` → "about me", "more about Niko", "background"
- `/log/theloop` → "The Agent is The Loop", "the loop post", "agent-loop analysis"
- (TBD pillar pages — fill in as they ship)
